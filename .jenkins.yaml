---
# OpenStack custom images for SUNET Platform
clean_workspace: true
triggers:
  cron: "@daily"

environment_variables:
  OPERATOR_VERSION: "0.1.7"
  PORTAL_VERSION: "0.1.21"

extra_jobs:
  # Keystone with OIDC support
  - name: openstack-keystone
    builders:
      - docker
    docker_name: platform/openstack-keystone
    docker_context_dir: keystone
    docker_tags:
      - "2025.1-ubuntu_noble-2"
      - "latest"

  # Keystone with OIDC support, OpenStack 2025.2 (test runs 2025.2
  # while prod is still on 2025.1; both lines need daily rebuilds)
  - name: openstack-keystone-2025.2
    builders:
      - docker
    docker_name: platform/openstack-keystone
    docker_context_dir: keystone-2025.2
    docker_tags:
      - "2025.2-ubuntu_noble-2"

  # Libvirt with QEMU SPICE support, OpenStack 2025.2
  - name: openstack-libvirt-2025.2
    builders:
      - docker
    docker_name: platform/openstack-libvirt
    docker_context_dir: libvirt-2025.2
    docker_tags:
      - "2025.2-ubuntu_noble-1"

  # OpenStack Operator (Kopf-based K8s operator)
  - name: openstack-operator
    builders:
      - docker
    docker_name: platform/openstack-operator
    docker_context_dir: openstack-operator
    docker_no_cache: true
    docker_tags:
      - "${OPERATOR_VERSION}"
      - "latest"

  # Customer self-service portal
  - name: customer-portal
    builders:
      - docker
    docker_name: platform/customer-portal
    docker_context_dir: customer-portal
    docker_no_cache: true
    docker_tags:
      - "${PORTAL_VERSION}"
      - "latest"

  # Managed customer-cluster VM and inventory provisioner
  - name: customer-cluster-operator
    builders:
      - docker
    docker_name: platform/customer-cluster-operator
    docker_context_dir: customer-cluster-operator
    docker_tags:
      - "0.1.5"
      - "latest"

  # Libvirt with QEMU SPICE support
  - name: openstack-libvirt
    builders:
      - docker
    docker_name: platform/openstack-libvirt
    docker_context_dir: libvirt
    docker_tags:
      - "2025.1-ubuntu_noble-1"
      - "latest"

  # Runner for the monitoring end-to-end tests (OpenStack client + kubectl)
  - name: monitoring-e2e
    builders:
      - docker
    docker_name: platform/monitoring-e2e
    docker_context_dir: monitoring-e2e
    docker_tags:
      - "0.1.0"
      - "latest"
